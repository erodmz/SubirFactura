<?xml version="1.0" encoding="utf-8"?>
<!--
  HTTP en claro SOLO contra la red local, para poder probar el API de
  desarrollo desde un teléfono real. Contra internet, únicamente HTTPS.

  Antes esto era usesCleartextTraffic="true" en el manifest: global, y por
  tanto también aplicable a producción. Ahora que el API vive en
  https://app.subirfactura.com no hay razón para dejar esa puerta abierta —
  una app que acepta HTTP en claro contra cualquier host es una app a la que
  se le puede colar un proxy en un wifi público.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Emulador de Android hacia el host -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Rangos privados: la Mac de desarrollo en la LAN -->
        <domain includeSubdomains="true">192.168.0.0/16</domain>
        <domain includeSubdomains="true">10.0.0.0/8</domain>
        <domain includeSubdomains="true">172.16.0.0/12</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
